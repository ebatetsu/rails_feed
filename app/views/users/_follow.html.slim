= form_for(current_user.active_relationships.build, html: { class: "user-follow-form" }, remote: true) do |f|
  div= hidden_field_tag :followed_id, @user.id
  = f.submit "フォローする", class: "btn btn-default"
